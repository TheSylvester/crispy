/**
 * Webview-level type definitions
 *
 * Shared types used across webview components, renderers, and hooks.
 *
 * @module webview/types
 */

/** Render mode for transcript entries */
export type RenderMode = 'yaml' | 'compact' | 'rich' | 'blocks';

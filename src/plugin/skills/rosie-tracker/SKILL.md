---
name: rosie-tracker
description: "Project tracking CLI for Rosie — view and manage tracked project state. Use when you want to inspect what Rosie is tracking, dump project state, or manually trigger tracker operations."
allowed-tools: Bash
---

# Rosie Tracker

CLI interface to the Rosie project tracker. View tracked state, inspect decisions, and manage project context.

## Usage

```bash
# Full usage reference
Bash(command: "node $CRISPY_TRACKER --help")
```

Refer to `--help` output for available commands and options.
